// min.js returns the smaller of two arguments
min=(a,b)=>{if(a<b){return a}else{return b}}
console.log(`the smaller number between the two is:`)
console.log(`0 and 10: ${min(0,10)}`)
console.log(`0 and -10: ${min(0,-10)}`)